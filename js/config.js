/**
 * App config. Set FORM_ENDPOINT to your form backend (e.g. Formspree URL) to enable real submission.
 */

export const config = {
  /** POST URL for contact form. Leave empty to only log to console. */
  formEndpoint: 'https://usebasin.com/f/624ada6a7f1c',
};
